#include<stdio.h>

void main(){

    // int x,y,z;

    // int num = scanf("%d %d", &x, &y);

    // print("%d", num);
    int date, month, year;
    printf("Enter DOB: ");
    scanf("%d-%d-%d",&date, &month, &year);
    printf("Date: %d  month: %d  year: %d",date, month, year);

    }