// Q4. Write a C program to delete all duplicate elements from an array.
#include <stdio.h>

int main() {

    int d;
    int arr[] = {1, 2, 2, 3, 4, 6, 6, 6};

    int len = sizeof(arr)/sizeof(int);

    for(int i=1;i<0;i++){
        d = arr[]
    }

    return 0;
}